<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <application
        android:allowBackup="true"
        android:label="XPrinter App"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault.Light.NoActionBar">
        
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- إضافة الـ Intent Filter الجديد للرد على الويب -->
            <intent-filter>
                <!-- الإجراء VIEW مهم ليستجيب للروابط -->
                <action android:name="android.intent.action.VIEW" />
                <!-- الفئة BROWSABLE تجعل التطبيق قابلاً للفتح من المتصفح -->
                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <!-- إعداد الرابط الخاص بالتطبيق (مثال: elsayeh://print) -->
                <data
                    android:scheme="elsayeh"
                    android:host="print" />
            </intent-filter>
        </activity>
    </application>
</manifest>